import { TicketListScreen } from '@/features/tickets/components/ticket-list-screen';

export const metadata = { title: 'Ticket CSKH — ERP' };

export default function Page() {
  return <TicketListScreen />;
}
