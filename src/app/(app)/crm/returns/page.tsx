import { RmaListScreen } from '@/features/orders/components/rma-list-screen';

export const metadata = { title: 'Đơn hoàn / RMA — ERP' };

export default function Page() {
  return <RmaListScreen />;
}
