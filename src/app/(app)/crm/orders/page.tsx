import { OrderListScreen } from '@/features/orders/components/order-list-screen';

export const metadata = { title: 'Đơn hàng — ERP' };

export default function Page() {
  return <OrderListScreen />;
}
