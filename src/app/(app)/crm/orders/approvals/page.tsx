import { ApprovalQueueScreen } from '@/features/orders/components/approval-queue-screen';

export const metadata = { title: 'Hàng chờ duyệt — ERP' };

export default function Page() {
  return <ApprovalQueueScreen />;
}
