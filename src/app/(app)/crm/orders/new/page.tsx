import { OrderCreateScreen } from '@/features/orders/components/order-create-screen';

export const metadata = { title: 'Tạo đơn hàng — ERP' };

export default function Page() {
  return <OrderCreateScreen />;
}
