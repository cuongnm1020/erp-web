import { CustomerListScreen } from '@/features/customers/components/customer-list-screen';

export const metadata = { title: 'Khách hàng — ERP' };

export default function Page() {
  return <CustomerListScreen />;
}
