import { MergeDuplicatesScreen } from '@/features/customers/components/merge-duplicates-screen';

export const metadata = { title: 'Gộp khách trùng — ERP' };

export default function Page() {
  return <MergeDuplicatesScreen />;
}
