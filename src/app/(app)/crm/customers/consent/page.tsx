import { MarketingConsentScreen } from '@/features/customers/components/marketing-consent-screen';

export const metadata = { title: 'Đồng ý marketing — ERP' };

export default function Page() {
  return <MarketingConsentScreen />;
}
