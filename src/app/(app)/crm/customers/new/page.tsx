import { CustomerFormScreen } from '@/features/customers/components/customer-form-screen';

export const metadata = { title: 'Tạo khách hàng — ERP' };

export default function Page() {
  return <CustomerFormScreen />;
}
