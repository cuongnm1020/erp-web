import { CustomerGroupsScreen } from '@/features/customers/components/customer-groups-screen';

export const metadata = { title: 'Nhóm khách hàng — ERP' };

export default function Page() {
  return <CustomerGroupsScreen />;
}
