import { PdaDevicesScreen } from '@/features/admin/components/pda-devices-screen';

export const metadata = { title: 'Thiết bị PDA — ERP' };

export default function Page() {
  return <PdaDevicesScreen />;
}
