import { WebhooksScreen } from '@/features/admin/components/webhooks-screen';

export const metadata = { title: 'Webhook & tích hợp — ERP' };

export default function Page() {
  return <WebhooksScreen />;
}
