import { SettingsScreen } from '@/features/admin/components/settings-screen';

export const metadata = { title: 'Cấu hình hệ thống — ERP' };

export default function Page() {
  return <SettingsScreen />;
}
