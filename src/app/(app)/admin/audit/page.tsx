import { AuditLogScreen } from '@/features/admin/components/audit-log-screen';

export const metadata = { title: 'Nhật ký hoạt động — ERP' };

export default function Page() {
  return <AuditLogScreen />;
}
