import { ImportScreen } from '@/features/admin/components/import-screen';

export const metadata = { title: 'Import khách hàng — ERP' };

export default function Page() {
  return <ImportScreen />;
}
