import { PancakeConfigScreen } from '@/features/admin/components/pancake-config-screen';

export const metadata = { title: 'Kết nối Pancake POS — ERP' };

export default function Page() {
  return <PancakeConfigScreen />;
}
