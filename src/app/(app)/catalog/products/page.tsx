import { ProductListScreen } from '@/features/catalog/components/product-list-screen';

export const metadata = { title: 'Sản phẩm — ERP' };

export default function Page() {
  return <ProductListScreen />;
}
