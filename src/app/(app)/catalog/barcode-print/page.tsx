import { BarcodePrintScreen } from '@/features/catalog/components/barcode-print-screen';

export const metadata = { title: 'In tem hàng loạt — ERP' };

export default function Page() {
  return <BarcodePrintScreen />;
}
