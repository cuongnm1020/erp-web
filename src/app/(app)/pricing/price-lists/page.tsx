import { PriceListsScreen } from '@/features/pricing/components/price-lists-screen';

export const metadata = { title: 'Bảng giá — ERP' };

export default function Page() {
  return <PriceListsScreen />;
}
