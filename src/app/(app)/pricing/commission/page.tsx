import { CommissionScreen } from '@/features/pricing/components/commission-screen';

export const metadata = { title: 'Hoa hồng — ERP' };

export default function Page() {
  return <CommissionScreen />;
}
