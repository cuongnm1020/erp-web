import { PackStationScreen } from '@/features/wms/components/pack-station-screen';

export const metadata = { title: 'Trạm đóng gói — ERP' };

export default function Page() {
  return <PackStationScreen />;
}
