import { StockLedgerScreen } from '@/features/wms/components/stock-ledger-screen';

export const metadata = { title: 'Sổ cái tồn — ERP' };

export default function Page() {
  return <StockLedgerScreen />;
}
