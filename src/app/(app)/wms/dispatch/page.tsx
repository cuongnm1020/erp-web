import { DispatchScreen } from '@/features/wms/components/dispatch-screen';

export const metadata = { title: 'Điều phối kho — ERP' };

export default function Page() {
  return <DispatchScreen />;
}
