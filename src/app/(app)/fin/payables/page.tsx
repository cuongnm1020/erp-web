import { PayablesAgingScreen } from '@/features/fin/components/payables-aging-screen';

export const metadata = { title: 'Công nợ phải trả — ERP' };

export default function Page() {
  return <PayablesAgingScreen />;
}
