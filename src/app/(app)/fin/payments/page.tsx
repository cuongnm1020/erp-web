import { PaymentsScreen } from '@/features/fin/components/payments-screen';

export const metadata = { title: 'Thu tiền & cấn trừ — ERP' };

export default function Page() {
  return <PaymentsScreen />;
}
