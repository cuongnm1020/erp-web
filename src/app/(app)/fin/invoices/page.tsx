import { InvoiceListScreen } from '@/features/fin/components/invoice-list-screen';

export const metadata = { title: 'Hóa đơn — ERP' };

export default function Page() {
  return <InvoiceListScreen />;
}
