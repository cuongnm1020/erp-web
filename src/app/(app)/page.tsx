import { DashboardScreen } from '@/features/dashboard/components/dashboard-screen';

export const metadata = { title: 'Tổng quan — ERP' };

export default function HomePage() {
  return <DashboardScreen />;
}
